function downloadFileEx(param1) {
      var url = 'https://api.latah.id.us/web/DownloadFileEx?filename=' + param1;
      window.open(url);
}
function downloadJobApp(filename){
      var url = 'https://api.latah.id.us/web/downloadJobApp?fileName=' + filename;
      window.open(url);
}